#pragma once

class FrameScanner
{
    struct node
    {
        //get this information from Dano08
    };

    private:

    FrameScanner();
    node getFrameInfo();
    void newFrameEntry(byte id[]);

}